/**
 * Storybook preset for a11y interaction tests
 */
module.exports = {
  // In a real addon, you might add babel or webpack configurations
  // Register the addon manager and/or decorator if needed
  managerEntries: [],
  // We don't need to add preview entries because these tests will be imported directly
  // into stories' play functions
};
